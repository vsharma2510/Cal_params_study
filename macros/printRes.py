#macro to run printResistances over multiple wp runs in a folder
import os
import glob

root_files_list = glob.glob("/nfs/cuore1/scratch/vsharma/Calibration_parameters_study/WP_measurements/*.root")

#print(root_files_list[0])


